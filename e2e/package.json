{
  "name": "@localhost-aliases/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "helper": "bun fixtures/fake-helper.ts"
  },
  "dependencies": { "@localhost-aliases/core": "workspace:*" },
  "devDependencies": { "@playwright/test": "^1.49.0" }
}
